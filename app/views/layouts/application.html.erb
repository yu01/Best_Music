<!DOCTYPE html>
<html>
  <head>
    <title>Best Music</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link rel="shortcut icon" href="/favicon.png"/>
    <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
  </head>

  <body>
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="my-container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <% if controller_name == "user" && action_name == "show"%>
            <a class="navbar-brand" href="#">Наверх</a>
          <% end %>
        </div>

        <% if controller_name == "user" %>
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
              <% if controller_name == "user" && action_name != "show"%>
                <li>
                  <%=link_to "Музыка", controller: "user", action: "show"%>
                </li>
              <% end %>
              <% if controller_name == "user" && action_name != "about"%>
                <li>
                  <%=link_to "О проекте", controller: "user", action: "about"%>
                </li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <% if controller_name == "tencet" || controller_name == "posts"%>
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
              <li>
                <%=link_to "Новые записи", controller: "tencet", action: "index"%>
              </li>
              <li>
                <%=link_to "Все записи", controller: "tencet", action: "all"%>
              </li>
            </ul>
          </div>
        <% end %>
      </div>
    </nav>
    <div class="body-content">
      <%= yield %>
    </div>
    <% if @footer_off.nil? && controller_name == "user" %>
      <footer>
        <div class="row">
          <div class="col-lg-12">
            <p class="footer-text">Copyright &copy; Белоречев Иван 2015</p>
          </div>
        </div>
      </footer>
    <% end %>
  </body>
</html>
